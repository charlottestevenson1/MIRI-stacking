# Redshift Bins

Run `Bin sorting.py` after the filter-object lists have been created.

The script matches catalogue redshifts to object IDs and creates one ID list for each
redshift bin in `Final/2. Redshift bins/Bin objects/`.