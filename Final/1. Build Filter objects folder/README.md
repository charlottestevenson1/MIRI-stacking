# Build Filter Objects

Before running the script, place the required catalogue FITS files in `Final/FITS files/`.
See `FITS files/README.md` for the required filenames.

Run:

```text
python "Final/1. Build Filter objects folder/filter objects builder.py"
```

This creates the per-filter object lists in `Final/Filter objects/`.