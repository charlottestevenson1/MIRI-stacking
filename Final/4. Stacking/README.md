# Inverse-Variance Stacking

Run `Inverse variance stacking.py` after the cutouts have been generated and masked.

The script performs inverse-variance stacking within each redshift bin and writes the
stacked science and error images to `Final/4. Stacking/Stacks/`.