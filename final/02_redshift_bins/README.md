# redshift_bins

Run `bin_sorting.py` after the filter-object lists have been created.

The script matches catalogue redshifts to object IDs and creates one ID list for each
redshift bin in `final/02_redshift_bins/bin_objects/`.