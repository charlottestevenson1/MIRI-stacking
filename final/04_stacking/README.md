# Inverse-Variance stacking

Run `inverse_variance_stacking.py` after the cutouts have been generated and masked.

The script performs inverse-variance stacking within each redshift bin and writes the
stacked science and error images to `final/04_stacking/stacks/`.