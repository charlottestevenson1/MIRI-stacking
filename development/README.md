# development Files

This folder contains older script versions and exploratory/testing scripts which are not required in the final pipeline. The production pipeline is in `final/`.

Most scripts use paths beginning with `development/` and should be run from the
repository root, `MIRI-stacking`.